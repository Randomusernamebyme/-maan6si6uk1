# 三個服務領域圖片上傳準備指南

## 需要準備的內容

### 1. **圖片文件準備**
準備三張圖片，對應三個服務領域：
- **生活助手** (河裡 - 全能工具人) - 建議尺寸：800x600px 或更大
- **社區拍檔** (小仙子拍檔) - 建議尺寸：800x600px 或更大  
- **街坊樹窿** (小松鼠) - 建議尺寸：800x600px 或更大

**圖片要求：**
- 格式：JPG、PNG 或 WebP
- 文件大小：建議每張不超過 2MB（用於網頁加載速度）
- 命名建議：
  - `生活助手.jpg` 或 `life-assistant.jpg`
  - `社區拍檔.jpg` 或 `community-partner.jpg`
  - `街坊樹窿.jpg` 或 `neighbor-tree-hole.jpg`

### 2. **Firebase Storage 配置檢查**
確認 `.env.local` 文件包含：
```
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
```

### 3. **上傳方式選擇**

#### 方式 A：使用 Firebase Console（最簡單）
1. 登入 [Firebase Console](https://console.firebase.google.com/)
2. 選擇你的項目
3. 進入 **Storage** 部分
4. 創建文件夾 `service-fields/`（如果不存在）
5. 上傳三張圖片到該文件夾
6. 獲取每張圖片的公開 URL（點擊圖片 → 複製 URL）

#### 方式 B：使用代碼上傳（需要開發）
- 需要安裝 Firebase Storage SDK（已包含在項目中）
- 需要創建上傳組件和 API 端點

### 4. **圖片存儲位置建議**
在 Firebase Storage 中的建議路徑：
```
service-fields/
  ├── 生活助手.jpg (或 life-assistant.jpg)
  ├── 社區拍檔.jpg (或 community-partner.jpg)
  └── 街坊樹窿.jpg (或 neighbor-tree-hole.jpg)
```

### 5. **需要更新的文件**
上傳圖片後，需要更新以下文件來顯示圖片：

1. **`components/home/ServicesSection.tsx`**
   - 將 emoji 圖標替換為圖片顯示
   - 使用 `<Image>` 組件顯示圖片

2. **`components/requests/RequestSubmissionForm.tsx`**
   - 在選擇字段時顯示對應的圖片

3. **`components/auth/RegisterForm.tsx`**
   - 在選擇服務範疇時顯示對應的圖片

### 6. **圖片 URL 存儲方式**

#### 選項 1：硬編碼在代碼中（簡單，適合固定圖片）
```typescript
const SERVICE_FIELD_IMAGES = {
  "生活助手": "https://firebasestorage.googleapis.com/.../生活助手.jpg",
  "社區拍檔": "https://firebasestorage.googleapis.com/.../社區拍檔.jpg",
  "街坊樹窿": "https://firebasestorage.googleapis.com/.../街坊樹窿.jpg",
};
```

#### 選項 2：存儲在 Firestore（靈活，可動態更新）
在 Firestore 創建文檔：
```
/serviceFields/images
  - 生活助手: "https://..."
  - 社區拍檔: "https://..."
  - 街坊樹窿: "https://..."
```

### 7. **下一步操作**
1. 準備好三張圖片
2. 上傳到 Firebase Storage（使用方式 A 或 B）
3. 獲取圖片 URL
4. 告訴我圖片 URL，我會幫你更新代碼來顯示這些圖片

## 快速開始（使用 Firebase Console）

1. **準備圖片**：確保三張圖片已準備好
2. **上傳圖片**：
   - 訪問 Firebase Console → Storage
   - 創建 `service-fields` 文件夾
   - 上傳三張圖片
3. **獲取 URL**：
   - 點擊每張圖片
   - 複製 "Download URL" 或 "gs://" URL
4. **提供給我**：
   - 將三個 URL 提供給我
   - 我會更新代碼來顯示這些圖片

## 注意事項

- 確保圖片有適當的版權許可
- 圖片應該清晰且符合品牌形象
- 考慮響應式設計，圖片在不同設備上都能正常顯示
- 建議使用 WebP 格式以獲得更好的壓縮和加載速度
