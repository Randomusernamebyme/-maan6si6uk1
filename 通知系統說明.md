# 通知系統說明文檔

## 概述

當前代碼庫中的通知系統是一個**部分實現**的系統。基礎架構已經準備好，包括數據模型、安全規則和 UI 佔位符，但實際的功能實現（API 端點、hooks、UI 組件）尚未完成。

---

## 1. 數據模型

### Notification 接口定義
位置：`types/index.ts`

```typescript
export interface Notification {
  // 基本資料
  id: string;                    // 文檔ID (主鍵)
  userId: string;               // 接收通知的用戶ID

  // 通知內容
  title: string;                // 通知標題
  message: string;               // 通知內容
  type: 'info' | 'success' | 'warning' | 'error';  // 通知類型

  // 關聯資料
  relatedRequestId?: string;     // 相關委托ID
  relatedApplicationId?: string; // 相關報名ID

  // 狀態
  read: boolean;                 // 是否已讀
  readAt?: Date;                 // 已讀時間

  // 系統欄位
  createdAt: Date;               // 創建時間
}
```

### Firestore 集合結構
- **集合名稱**：`notifications`
- **文檔結構**：符合 `Notification` 接口

---

## 2. 安全規則

位置：`firestore.rules`

```javascript
// 通知集合
match /notifications/{notificationId} {
  // 用戶只能讀取自己的通知
  allow read: if request.auth != null && resource.data.userId == request.auth.uid;
  // 只有管理員可以創建通知
  allow write: if isAdmin();
}
```

### 權限說明：
- **讀取**：用戶只能讀取自己的通知（`userId` 匹配當前登入用戶）
- **寫入**：只有管理員可以創建、更新或刪除通知

---

## 3. UI 顯示（部分實現）

### 義工儀表板
位置：`app/(dashboard)/volunteer/dashboard/page.tsx`

**已實現：**
- 顯示未讀通知數量的卡片 UI
- 統計數據中包含 `unreadNotifications`

**未實現：**
- 實際獲取未讀通知數量的邏輯（目前硬編碼為 `0`）
- 點擊通知卡片跳轉到通知列表
- 通知列表頁面

```typescript
// 當前代碼（第 86-87 行）
// TODO: 獲取未讀通知數量
const unreadNotifications = 0;
```

---

## 4. 缺失的功能

### 4.1 API 端點（未實現）

需要創建以下 API 端點：

#### `GET /api/notifications`
- **功能**：獲取當前用戶的通知列表
- **查詢參數**：
  - `read` (可選)：篩選已讀/未讀通知
  - `type` (可選)：篩選通知類型
  - `limit` (可選)：限制返回數量
- **返回**：通知數組

#### `GET /api/notifications/[id]`
- **功能**：獲取單個通知詳情
- **返回**：通知對象

#### `PATCH /api/notifications/[id]`
- **功能**：標記通知為已讀
- **請求體**：`{ read: true, readAt: Date }`
- **返回**：更新後的通知

#### `PATCH /api/notifications/read-all`
- **功能**：標記所有通知為已讀
- **返回**：成功狀態

#### `POST /api/admin/notifications`（管理員專用）
- **功能**：創建新通知
- **請求體**：符合 `Notification` 接口（除 `id`, `createdAt`）
- **返回**：創建的通知

### 4.2 React Hooks（未實現）

需要創建以下 hooks：

#### `useNotifications(userId: string)`
- **功能**：獲取用戶的所有通知
- **返回**：
  ```typescript
  {
    notifications: Notification[];
    unreadCount: number;
    loading: boolean;
    error: Error | null;
  }
  ```

#### `useNotification(notificationId: string)`
- **功能**：獲取單個通知詳情
- **返回**：
  ```typescript
  {
    notification: Notification | null;
    loading: boolean;
    error: Error | null;
  }
  ```

### 4.3 UI 組件（未實現）

需要創建以下組件：

#### 通知列表頁面
- **路徑**：`app/(dashboard)/volunteer/notifications/page.tsx`
- **功能**：
  - 顯示所有通知列表
  - 篩選已讀/未讀
  - 標記為已讀
  - 點擊通知查看詳情或跳轉到相關頁面

#### 通知卡片組件
- **位置**：`components/notifications/NotificationCard.tsx`
- **功能**：顯示單個通知的卡片

#### 通知下拉菜單（Header）
- **功能**：在 Header 中顯示通知圖標和未讀數量
- **點擊**：展開通知列表

---

## 5. 通知觸發場景（建議）

以下場景應該觸發通知創建：

### 5.1 委托相關
- 委托狀態變更（已批准、已發布、已配對、已完成）
- 委托被取消

### 5.2 報名相關
- 報名被批准（義工收到通知）
- 報名被拒絕（義工收到通知）
- 報名狀態變更為已完成

### 5.3 義工相關
- 義工狀態變更（已批准、已拒絕、已暫停）

### 5.4 系統通知
- 新功能發布
- 重要公告

---

## 6. 實現優先級建議

### 高優先級
1. ✅ 創建 `useNotifications` hook
2. ✅ 創建 `GET /api/notifications` API 端點
3. ✅ 實現義工儀表板中的未讀通知數量
4. ✅ 創建通知列表頁面

### 中優先級
5. ✅ 創建通知卡片組件
6. ✅ 實現標記為已讀功能
7. ✅ 在 Header 中添加通知圖標

### 低優先級
8. ✅ 實現通知創建邏輯（在各個 API 端點中）
9. ✅ 添加通知推送（如果需要的話）
10. ✅ 通知過濾和排序功能

---

## 7. 當前狀態總結

| 組件 | 狀態 | 說明 |
|------|------|------|
| 數據模型 | ✅ 完成 | `Notification` 接口已定義 |
| Firestore 規則 | ✅ 完成 | 安全規則已設置 |
| UI 佔位符 | ⚠️ 部分完成 | 顯示未讀數量，但數據為硬編碼 |
| API 端點 | ❌ 未實現 | 需要創建所有 API 端點 |
| React Hooks | ❌ 未實現 | 需要創建 `useNotifications` 等 |
| 通知列表頁面 | ❌ 未實現 | 需要創建完整頁面 |
| 通知創建邏輯 | ❌ 未實現 | 需要在相關操作中觸發通知 |

---

## 8. 技術實現建議

### 8.1 使用 Firestore 實時監聽
建議使用 `onSnapshot` 來實時獲取通知更新：

```typescript
// 在 useNotifications hook 中
useEffect(() => {
  if (!userId) return;
  
  const q = query(
    collection(db, "notifications"),
    where("userId", "==", userId),
    orderBy("createdAt", "desc")
  );
  
  const unsubscribe = onSnapshot(q, (snapshot) => {
    // 處理通知更新
  });
  
  return () => unsubscribe();
}, [userId]);
```

### 8.2 通知創建輔助函數
創建一個輔助函數來統一創建通知：

```typescript
// lib/utils/notifications.ts
export async function createNotification(
  userId: string,
  title: string,
  message: string,
  type: Notification['type'],
  relatedRequestId?: string,
  relatedApplicationId?: string
) {
  // 使用 Admin SDK 創建通知
}
```

---

## 結論

當前通知系統的**基礎架構已準備就緒**，但**實際功能尚未實現**。要完成通知系統，需要：

1. 實現 API 端點（獲取、更新通知）
2. 創建 React Hooks（數據獲取）
3. 創建 UI 組件（通知列表、通知卡片）
4. 在相關操作中觸發通知創建

這是一個典型的"準備好但未實現"的功能，可以根據優先級逐步實現。
