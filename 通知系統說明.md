# 通知系統說明

## 概述

根據代碼庫中的類型定義和 Firestore 規則，通知系統是一個用於向用戶發送重要消息和更新的功能。目前系統已經定義了通知的數據結構和安全規則，但實際的實現文件已被移除。

## 通知系統的用途

通知系統設計用於：

1. **向義工發送重要更新**
   - 報名狀態變更（已選中、未選中、已完成）
   - 委托狀態更新
   - 系統消息

2. **向管理員發送提醒**
   - 新委托提交
   - 新義工註冊
   - 需要審核的項目

3. **用戶互動通知**
   - 委托配對成功
   - 委托完成確認
   - 其他重要事件

## 數據結構

根據 `types/index.ts` 中的定義，通知包含以下字段：

```typescript
interface Notification {
  id: string;                    // 文檔ID (主鍵)
  userId: string;                // 接收通知的用戶ID
  
  // 通知內容
  title: string;                 // 通知標題
  message: string;               // 通知內容
  type: 'info' | 'success' | 'warning' | 'error';  // 通知類型
  
  // 關聯資料
  relatedRequestId?: string;    // 相關委托ID
  relatedApplicationId?: string; // 相關報名ID
  
  // 狀態
  read: boolean;                 // 是否已讀
  readAt?: Date;                 // 已讀時間
  
  // 系統欄位
  createdAt: Date;               // 創建時間
}
```

## 安全規則

根據 `firestore.rules`：

- **讀取權限**：用戶只能讀取自己的通知
  ```javascript
  allow read: if request.auth != null && resource.data.userId == request.auth.uid;
  ```

- **寫入權限**：只有管理員可以創建通知
  ```javascript
  allow write: if isAdmin();
  ```

- **更新權限**：用戶可以更新自己的通知（標記為已讀）

## 顯示位置（計劃中）

根據 `app/(dashboard)/volunteer/dashboard/page.tsx` 的代碼，通知系統計劃在以下位置顯示：

1. **義工儀表板**
   - 統計卡片中顯示"未讀通知"數量
   - 目前顯示為 0（TODO 註釋表示需要實現）

2. **通知頁面**（已移除）
   - 原本應該有一個專門的通知頁面來顯示所有通知
   - 用戶可以標記通知為已讀
   - 可以點擊通知跳轉到相關的委托或報名詳情

## 當前狀態

### 已實現的部分：
- ✅ 通知數據類型定義（`types/index.ts`）
- ✅ Firestore 安全規則（`firestore.rules`）
- ✅ 義工儀表板中的未讀通知統計占位符

### 已移除的部分：
- ❌ 通知 API 端點（`/api/notifications/route.ts`）
- ❌ 通知頁面（`/app/(dashboard)/volunteer/notifications/page.tsx`）
- ❌ 通知組件（`components/notifications/NotificationCard.tsx`）
- ❌ 通知 Hook（`lib/hooks/useNotifications.ts`）
- ❌ 通知工具函數（`lib/utils/notifications.ts`）

## 使用場景（計劃中）

通知系統應該在以下情況下自動創建通知：

1. **義工報名被選中時**
   - 當管理員將報名狀態改為 "approved"
   - 通知類型：`success`
   - 包含相關委托和報名 ID

2. **義工報名被拒絕時**
   - 當管理員將報名狀態改為 "rejected"
   - 通知類型：`warning`
   - 包含相關委托和報名 ID

3. **委托完成時**
   - 當委托狀態改為 "completed"
   - 通知類型：`success`
   - 包含相關委托 ID

4. **新委托發布時**
   - 當委托狀態改為 "published"
   - 通知類型：`info`
   - 發送給所有已批准的義工

5. **義工註冊被批准時**
   - 當義工狀態改為 "approved"
   - 通知類型：`success`

## 之前的實現（已移除）

根據 Git 歷史記錄，通知系統之前已經完整實現過，包括：

### API 端點（已移除）：
1. **`GET /api/notifications`** - 獲取當前用戶的通知列表
   - 支持篩選參數：`read` (true/false), `type`, `limit`
   - 按創建時間倒序排列
   - 只返回當前用戶的通知

2. **`GET /api/notifications/[id]`** - 獲取單個通知詳情

3. **`PATCH /api/notifications/[id]`** - 標記通知為已讀
   - 更新 `read: true` 和 `readAt: new Date()`

4. **`PATCH /api/notifications/read-all`** - 標記所有通知為已讀
   - 批量更新當前用戶的所有未讀通知

5. **`POST /api/admin/notifications`** - 創建通知（管理員專用）
   - 管理員可以為任何用戶創建通知

### 工具函數（已移除）：
- **`lib/utils/notifications.ts`**
  - `createNotification()` - 創建單個通知
  - `createNotifications()` - 批量創建通知
  - 使用 Admin SDK 在服務器端創建通知

### React Hook（已移除）：
- **`lib/hooks/useNotifications.ts`**
  - 獲取用戶通知列表
  - 支持 Firestore 實時監聽
  - 如果權限不足，回退到 API 輪詢

### UI 組件（已移除）：
- **`components/notifications/NotificationCard.tsx`**
  - 顯示單個通知卡片
  - 根據通知類型顯示不同顏色
  - 可點擊跳轉到相關委托或報名

- **`app/(dashboard)/volunteer/notifications/page.tsx`**
  - 通知列表頁面
  - 支持篩選（全部/未讀/已讀）
  - 支持標記為已讀/全部已讀
  - 顯示通知詳情和相關鏈接

## 實現建議

如果要重新實現通知系統，需要：

1. **恢復 API 端點**
   - 參考之前的實現（commit bc19a7b）
   - 確保使用 Admin SDK 進行權限驗證

2. **恢復通知頁面**
   - `/app/(dashboard)/volunteer/notifications/page.tsx`
   - 顯示所有通知列表
   - 支持篩選（全部/未讀/已讀）
   - 支持標記為已讀/全部已讀

3. **恢復通知組件**
   - `components/notifications/NotificationCard.tsx` - 通知卡片
   - 根據通知類型顯示不同樣式

4. **恢復 Hook**
   - `lib/hooks/useNotifications.ts` - 獲取和管理通知
   - 支持實時更新和 API 回退

5. **恢復工具函數**
   - `lib/utils/notifications.ts` - 創建通知的輔助函數

6. **在相關操作中創建通知**
   - 在報名狀態更新時（`/api/applications/[id]/route.ts`）
   - 在委托狀態更新時（`/api/admin/requests/[id]/route.ts`）
   - 在義工狀態更新時（`/api/admin/volunteers/[id]/route.ts`）

7. **在 Header 或導航中添加通知圖標**
   - 顯示未讀通知數量
   - 點擊跳轉到通知頁面

8. **更新義工儀表板**
   - 實現未讀通知數量的實際獲取（目前是 TODO）
   - 添加通知卡片或鏈接

## 總結

通知系統的基礎架構（類型定義和安全規則）已經存在，但實際的實現文件已被移除。系統設計用於向用戶發送重要更新和消息，但目前處於未實現狀態。如果需要使用通知功能，需要重新實現上述提到的所有組件和 API 端點。
