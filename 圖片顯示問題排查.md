# 圖片顯示問題排查指南

## 已完成的更新

1. ✅ 更新了圖片 URL：
   - 生活助手: `1.png`
   - 社區拍檔: `3.png` (已更新)
   - 街坊樹窿: `2.png`

2. ✅ 添加了錯誤處理和調試日誌

## 如果圖片仍然不顯示，請檢查以下幾點：

### 1. Firebase Storage 權限設置

**重要：圖片必須設置為公開讀取才能顯示！**

#### 檢查步驟：
1. 登入 [Firebase Console](https://console.firebase.google.com/)
2. 選擇你的項目 `maan6si6uk1`
3. 進入 **Storage** 部分
4. 進入 `service-fields/` 文件夾
5. 點擊每張圖片（1.png, 2.png, 3.png）
6. 檢查 **權限** 標籤

#### 設置公開讀取權限：
1. 在 Storage 規則中，確保有以下規則：
```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 允許公開讀取 service-fields 文件夾中的圖片
    match /service-fields/{image} {
      allow read: if true;  // 公開讀取
      allow write: if request.auth != null;  // 需要登入才能寫入
    }
  }
}
```

2. 或者，在 Firebase Console 中：
   - 點擊圖片
   - 在 **權限** 標籤中
   - 添加規則：`allUsers` 具有 `Reader` 權限

### 2. 驗證圖片 URL 是否可訪問

在瀏覽器中直接訪問以下 URL，看是否能下載圖片：

- 生活助手: `https://firebasestorage.googleapis.com/v0/b/maan6si6uk1.appspot.com/o/service-fields%2F1.png?alt=media`
- 社區拍檔: `https://firebasestorage.googleapis.com/v0/b/maan6si6uk1.appspot.com/o/service-fields%2F3.png?alt=media`
- 街坊樹窿: `https://firebasestorage.googleapis.com/v0/b/maan6si6uk1.appspot.com/o/service-fields%2F2.png?alt=media`

**如果這些 URL 返回 403 Forbidden 或 404 Not Found：**
- 說明權限未設置或圖片路徑不正確
- 需要檢查 Firebase Storage 規則和圖片實際路徑

### 3. 獲取正確的公開 URL

#### 方法 A：從 Firebase Console 獲取
1. 在 Firebase Console → Storage
2. 點擊圖片文件
3. 複製 **Download URL**（不是 gs:// URL）
4. 這個 URL 應該可以直接在瀏覽器中訪問

#### 方法 B：使用 Firebase Storage 規則生成 URL
如果圖片已設置為公開，URL 格式應該是：
```
https://firebasestorage.googleapis.com/v0/b/{bucket}/o/{encoded-path}?alt=media
```

其中：
- `{bucket}` = `maan6si6uk1.appspot.com`
- `{encoded-path}` = URL 編碼的路徑，例如 `service-fields%2F1.png`

### 4. 檢查瀏覽器控制台

打開瀏覽器開發者工具（F12），查看：
1. **Console** 標籤：是否有圖片加載錯誤
2. **Network** 標籤：檢查圖片請求的狀態碼
   - 200 = 成功
   - 403 = 權限不足
   - 404 = 文件不存在

### 5. 臨時解決方案：使用公開 URL

如果 Firebase Storage 權限設置有問題，可以：
1. 在 Firebase Console 中獲取每張圖片的 **Download URL**
2. 將這些 URL 提供給我，我會更新代碼

### 6. 測試圖片 URL

在終端中運行以下命令測試圖片是否可訪問：

```bash
# 測試生活助手圖片
curl -I "https://firebasestorage.googleapis.com/v0/b/maan6si6uk1.appspot.com/o/service-fields%2F1.png?alt=media"

# 測試社區拍檔圖片
curl -I "https://firebasestorage.googleapis.com/v0/b/maan6si6uk1.appspot.com/o/service-fields%2F3.png?alt=media"

# 測試街坊樹窿圖片
curl -I "https://firebasestorage.googleapis.com/v0/b/maan6si6uk1.appspot.com/o/service-fields%2F2.png?alt=media"
```

如果返回 `HTTP/2 200`，說明圖片可以訪問。
如果返回 `HTTP/2 403`，說明需要設置權限。

## 快速修復步驟

1. **設置 Firebase Storage 規則為公開讀取**
2. **驗證圖片 URL 可以在瀏覽器中直接訪問**
3. **清除瀏覽器緩存並重新加載頁面**
4. **檢查瀏覽器控制台是否有錯誤信息**

## 如果問題仍然存在

請提供：
1. 瀏覽器控制台的錯誤信息
2. Network 標籤中圖片請求的狀態碼
3. 從 Firebase Console 獲取的實際 Download URL

這樣我可以幫你進一步排查問題。
